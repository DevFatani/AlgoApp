<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/b_dark_purple</item>
        <item name="colorPrimaryDark">@color/b_dark_purple</item>
    </style>

    <style name="AlogBtn">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/font_small</item>
        <item name="android:layout_marginTop">@dimen/regular_margin</item>
        <item name="android:layout_marginBottom">@dimen/regular_margin</item>
        <item name="android:layout_marginLeft">@dimen/regular_margin</item>
        <item name="android:layout_marginRight">@dimen/regular_margin</item>
    </style>

    <style name="AlgoEt">
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">@dimen/regular_margin</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/font_small</item>
    </style>


    <style name="FullscreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/s_dark_red</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
</resources>
